☐ Set up script to allow for different endpoints to be accessed. @started(21-01-19 14:13)
☐ set up examples of code that a user can then learn and use within their own code.
☐ add complex config, meaning the user can add optional . chains to get a request